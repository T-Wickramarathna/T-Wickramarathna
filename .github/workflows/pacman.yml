name: generate-pacman-svg

on:
  schedule: # Runs every 24 hours
    - cron: "0 0 * * *"
  workflow_dispatch: # Allows manual run

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: generate-pacman-svg
        uses: productive-cpp/contribution-pacman@main
        with:
          github_user_name: T-Wickramarathna
          # The output will be saved to a branch named "output"
